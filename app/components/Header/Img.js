import styled from 'styled-components';

import NormalImg from 'components/Img';

const Img = styled(NormalImg)`
  width: 380px;
  height: inherit;
  float: left;
  padding: 0;
  margin: 0;
`;

export default Img;
